services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - clb_data:/data
    environment:
      - DATABASE_URL=sqlite:////data/clb.db
    restart: unless-stopped

volumes:
  clb_data:
